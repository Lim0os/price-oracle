version: "2"

run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - revive
    - ineffassign
    - unused

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-use-default: false
